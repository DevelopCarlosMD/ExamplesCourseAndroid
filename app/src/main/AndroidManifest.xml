<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hassassin.com.viewexamples" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalculadoraLinear"
                    android:label="@string/app_name"/>
        <activity android:name=".TabAdapter"
                  android:label="@string/app_name"/>

        <activity android:name=".TabActivity1"/>
        <activity android:name=".TabActivity2"/>
        <activity android:name=".TabActivity3"/>
        <activity android:name=".ViewFlipAdapter"
                 android:label="@string/app_name"/>

    </application>

</manifest>
